<?php

use Illuminate\Support\Facades\Route;
use Erp\{{moduleName}}\App\Http\Controllers\Api\{{moduleName}}Controller;

Route::group(
    ['middleware' => ['api', 'auth:api'], 'prefix' => 'api'],
    function () {
        Route::apiResource('{{moduleRouteKey}}', ModuleController::class);
        // Add more API routes here
    }
);
